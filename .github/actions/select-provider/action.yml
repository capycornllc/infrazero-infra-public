name: Select cloud provider
description: >-
  Provider-neutral entrypoint. Resolves directories by convention, validates
  the selected provider contract and publishes provider paths for later steps.

runs:
  using: composite
  steps:
    - name: Resolve provider
      shell: bash
      run: bash scripts/common/select-provider.sh
